spring:
  application:
    name: snippet-searcher-app
  datasource:
    url: jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://dev-65tlgyzfpbzeka8a.us.auth0.com/
  redis:
    host: ${REDIS_HOST}
    port: ${REDIS_PORT}
redis:
  stream:
    key: "jobs"

app:
  accessmanager:
    url: http://${ACCESS_MANAGER_HOST}:${ACCESS_MANAGER_PORT}

server:
  port: 8080

auth0:
  domain: dev-65tlgyzfpbzeka8a.us.auth0.com
  audience: https://api.snippets.com
  clientId: WlTG30qceM7qOiDY0hB90bVtUog4xCdp
  clientSecret: gR_X5kVh5bjLPwqh9IHLE2aSwKFu5pkQ-MoIaN8Yjk_NIsAWbonq47IpgyoPjzPX


# Configuración de logs con request-id
logging:
  pattern:
    console: "[%X{request_id}] %d{yyyy-MM-dd HH:mm:ss} - %msg%n"
  level:
    root: INFO
    com.grupo14IngSis.snippetSearcher: DEBUG
